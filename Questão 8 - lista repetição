#include <stdio.h>

int main()
{
    int i, numero, soma=0;

    for(i=0; i<10; i++){
        printf("Digite o %d° numero: ", i+1);
        scanf("%d", &numero);
        soma += numero;
    }

    printf("Media: %.2f", (float)soma/10);

    return 0;
}
