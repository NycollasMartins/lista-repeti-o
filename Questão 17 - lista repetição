#include <stdio.h>

int main() {
  int N;
  int k = 1;
  int i;

  printf("Digite um inteiro positivo N: ");
  scanf("%d", &N);

  for (i = 1; i <= N; i++) {
    int j;
    for (j = 1; j <= i; j++) {
      printf("%d ", k);
      k++;
    }
    printf("\n");
  }

  return 0;
}
